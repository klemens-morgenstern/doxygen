
project translations ;

import gettext ;

#rule make_mo_target ( name ) 
#{
#	return [ gettext.catalog $(name) : $(name).po ] ;
#}
#
local lst = [ glob *.po ] ;

local res ;

for local l in $(lst) 
{
	import regex ;
	
	local lb = $(l:B) ;
	local r = [ regex.match doxy_(.*) : $(lb) ] ; 
   
	local tar =  $(r)/LC_MESSAGES/doxy ;
	gettext.catalog $(tar) : $(lb).po : <location>bin ;
	
	res = $(res) $(tar) ;	
} 

alias translations ; #: $(res) ;

